[package]
name = "gfwd"
version = "0.1.0"
edition = "2024"

[dependencies]
relm4 = { version = "0.9.1", features = ["libadwaita", "gnome_47"] }
relm4-components = { version = "0.9.1", features = ["libadwaita"] }
tracker = "0.2.2"

gfwd-bus = { path = "../gfwd-bus", features = ["firewalld1", "config_firewalld1"]}
tokio = { version = "1.45.1", features = ["sync"] }
zbus = "5.7.1"

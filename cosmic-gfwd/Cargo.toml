[package]
name = "cosmic-gfwd"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "A libcosmic GUI for Firewalld made in Rust"
repository = "https://github.com/pop-os/cosmic-app-template"

[dependencies]
futures-util = "0.3.33"
gfwd-bus = { path = "../gfwd-bus", features = ["config_firewalld1", "config_icmptype", "config_ipset", "config_service", "config_zone", "firewalld1", "network_manager", "systemd", "zone"] }
i18n-embed = { version = "0.16", features = [
    "fluent-system",
    "desktop-requester",
] }
i18n-embed-fl = "0.10.1"
open = "5.4.0"
rust-embed = "8.12.0"
slotmap = "1.1.1"
tokio = { version = "1.53.1", features = ["full"] }
zbus = "5.18.0"

[dependencies.libcosmic]
git = "https://github.com/pop-os/libcosmic.git"
# See https://github.com/pop-os/libcosmic/blob/master/Cargo.toml for available features.
features = [
    # Accessibility support
    "a11y",
    # About widget for the app
    "about",
    # Uses cosmic-settings-daemon to watch for config file changes
    "dbus-config",
    # Support creating additional application windows.
    "multi-window",
    # On app startup, focuses an existing instance if the app is already open
    "single-instance",
    # Uses tokio as the executor for the runtime
    "tokio",
    # Windowing support for X11, Windows, Mac, & Redox
    "winit",
    # Add Wayland support to winit
    "wayland",
    # GPU-accelerated rendering
    "wgpu",
]

# Uncomment to test a locally-cloned libcosmic
# [patch.'https://github.com/pop-os/libcosmic']
# libcosmic = { path = "../libcosmic" }
# cosmic-config = { path = "../libcosmic/cosmic-config" }
# cosmic-theme = { path = "../libcosmic/cosmic-theme" }
